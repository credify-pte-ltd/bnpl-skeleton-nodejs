import type { OIDCOptions } from "../types";
import { CredifyClass } from "./common";
export declare class OIDC extends CredifyClass {
    /**
     * Generates a new OIDC URL.
     * For data consumer
     * @param id - Organization ID hosting this server
     * @param redirectUrl
     * @param scopes
     * @param options
     * @returns { oidcUrl: string, privateKey: string } - this private key is base64 URL encoded data.
     */
    initiateOIDC(id: string, redirectUrl: string, scopes: string[], options?: OIDCOptions): Promise<{
        oidcUrl: string;
        privateKey: string;
    }>;
    /**
     * Generates a new access token out of an authorization code.
     * @param id - client ID
     * @param code - Authorization code
     * @param redirectUri
     */
    generateAccessToken(id: string, code: string, redirectUri: string): Promise<object>;
    /**
     * Gets userinfo from OIDC
     * For data consumer
     * @param accessToken
     * @param privateKey - Base64 URL encoded private key.
     */
    userinfo(accessToken: string, privateKey: string): Promise<object>;
}
