import { CredifyClass } from "./common";
import type { Profile, Keys } from "../types";
export interface EntityCreationPayload {
    profile: Profile;
    signingPublicKey: string;
    signingSecret: string;
    encryptionPublicKey: string;
    encryptionSecret: string;
    password: string;
}
export declare class Entity extends CredifyClass {
    private validate;
    private trim;
    /**
     * Creates a new individual entity.
     * @param profile
     * @param password
     * @param keys - Key pairs can be passed from frontend.
     */
    create(profile: Profile, password: string, keys?: Keys): Promise<string>;
}
